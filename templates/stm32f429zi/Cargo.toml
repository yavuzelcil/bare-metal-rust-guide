[package]
name = "stm32f429zi-project"
version = "0.1.0"
edition = "2021"

[dependencies]
# cortex-m = "0.7"
# cortex-m-rt = "0.7"
# panic-halt = "0.2"

[profile.release]
codegen-units = 1
lto = true
opt-level = "z"  # veya "s" (size optimization)
panic = "abort"
