[package]
name = "rp2040-project"
version = "0.1.0"
edition = "2021"

[dependencies]
# cortex-m = "0.7"
# cortex-m-rt = "0.7"
# rp2040-boot2 = "0.3"  # Second stage bootloader
# panic-halt = "0.2"

[profile.release]
codegen-units = 1
lto = true
opt-level = "z"
panic = "abort"
